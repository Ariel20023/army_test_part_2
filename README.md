# army_test_part_2
In this project, the server asks questions of the database and returns the results to the user. To run the system, you need to enter the command 
docker compose up in the main file of the project (army_test_part_2). 
Then, go to http://localhost:9000/docs in your browser and the routes will be displayed in a nice and convenient way.